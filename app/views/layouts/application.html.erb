<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : " Epsilon project" %></title>
    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>

  </head>

  <body>
    <% if notice %>
      <div data-alert class="alert-box info radius">
        <%= notice %>
        <%= link_to  'x', "#", class: "close"  %>
      </div>
    <% end %>
    
    <% if alert %>
      <div data-alert class="alert-box warning radius">
        <%= alert %>
        <%= link_to 'x', "#", class: "close" %>
      </div>
    <% end %>
  
  <nav class="top-bar" data-topbar>
    <ul class="title-area">
      <li class="name">
        <h1><link_to 'Epsilon project', root_path>Epsilon project</a></h1>
      </li>
      <!-- Remove the class "menu-icon" to get rid of menu icon. Take out "Menu" to just have icon alone -->
      <li class="toggle-topbar menu-icon"><a href="#"><span>Menu</span></a></li>
    </ul>
    <!-- Top navigation menu -->
    <section class="top-bar-section">
      <!-- Right Nav Section -->
      <ul class="right">
      <% if user_signed_in? %>        
        <li class="has-dropdown">
          <a>Logged in as <strong><%= current_user.email %></strong></a>
          <ul class="dropdown">
            <li><%= link_to 'Edit profile', edit_user_registration_path %></li>
            <li><%= link_to "Logout", destroy_user_session_path, method: :delete %></li>
          </ul>
        </li>
      <% end %>
      </ul>
    </section>
  </nav>

  <!--Main content area-->
  <div class="row">
    <div class="small-9 columns"><h2>Welcome to Epsilon: Place what counts</h2></div>
    <div class="small-3 columns"></div>
  </div>
    <div class="row">
    <div class="small-9 columns"><%= yield %></div>
    <div class="small-3 columns">
      <h3>Tip of the day:</h3>
      <p>Here we will post some random tips</p>
    </div>
  </div>
    
    <%= javascript_include_tag "application" %>
  </body>
</html>
